server:
  port: 8082

#Values
jwt:
  secret: Qf8rXyP7vT3nWz9aL0kR2uM6sD1gJ5bH4yV8cE7pA0mN9qS2tU3wX6zF1hK8lC5
  expiration: 3600000

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        # Route to Auth Service
        - id: auth-service
          uri: lb://AUTH-SERVICE
          predicates:
            - Path=/api/auth/**

        # Route to Execution Service
        - id: execution-service
          uri: lb://EXECUTION-SERVICE
          predicates:
            - Path=/api/connection/**,/api/execute/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
